[project]
name = "mini-sdp"
version = "0.1.0"
description = "(Mini-)SDP for SKAO technical assessment"
readme = "README.md"
authors = [
    { name = "SimonP2207", email = "simonp2207@gmail.com" }
]
requires-python = ">=3.14"
dependencies = [
    "astropy>=8.0.1",
    "fastapi>=0.141.1",
    "matplotlib>=3.11.2",
    "pydantic>=2.13.5",
]

[project.scripts]
mini-sdp = "mini_sdp:main"
mini-sdp-generate = "mini_sdp.generator:main"

[build-system]
requires = ["uv_build>=0.12.9,<0.13.0"]
build-backend = "uv_build"

[dependency-groups]
dev = [
    "httpx>=0.28.1",
    "pytest>=9.1.1",
    "pytest-asyncio>=1.4.0",
    "ruff>=0.16.8",
]

[tool.pytest.ini_options]
testpaths = ["src/mini_sdp/tests"]
asyncio_mode = "auto"

[tool.ruff]
extend-exclude = ["docs/lib"]